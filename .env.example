# Fuel iX (TELUS) — OpenAI-compatible AI gateway used by CertBot
# Get an API key from your Fuel iX console.
VITE_FUELIX_API_KEY=

# Optional — override the base URL or model if your tenant differs.
# VITE_FUELIX_BASE_URL=https://api.fuelix.ai
# VITE_FUELIX_MODEL=gpt-4o-mini
